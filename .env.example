# ReelHuntr — environment variables
#
# All keys are read SERVER-SIDE only (never shipped to the client).
# The aggregator falls through providers in order: TMDB → OMDb → local mock.
# Missing keys are OK — the layer is simply skipped and the next one is tried.

# --- TMDB (primary source, includes JustWatch watch/providers) ---
# Get one at https://www.themoviedb.org/settings/api
# Use ONE of the two:
TMDB_API_KEY=
# OR the v4 read-access token (preferred):
TMDB_READ_TOKEN=

# --- OMDb (fallback for metadata when TMDB is unavailable) ---
# Get one at https://www.omdbapi.com/apikey.aspx
OMDB_API_KEY=

# --- Public site URL (used for canonical, OG, sitemap, robots) ---
NEXT_PUBLIC_SITE_URL=https://reelhuntr.com

# --- Amazon Associates (affiliate links on Prime Video results) ---
# Get your tag at https://affiliate-program.amazon.com
# Format: yourname-20 (US), yourname-21 (most other countries)
AMAZON_AFFILIATE_TAG=

# --- Apple Performance Partners (affiliate links on Apple TV+ results) ---
# Apply at https://performance-partners.apple.com
# Your token is shown in Impact under Manage > Tracking Parameters
APPLE_AFFILIATE_TOKEN=

# --- Google AdSense (display ads) ---
# Master switch — ads only load when this is exactly "true". Keep it off
# until the AdSense account is actually serving ads (otherwise AdSense only
# injects an empty event frame that trips a harmless CSP "Framing ''" warning).
NEXT_PUBLIC_ADS_ENABLED=
# Publisher ID from https://www.google.com/adsense — format: ca-pub-XXXXXXXXXXXXXXXXX
NEXT_PUBLIC_ADSENSE_ID=
# Slot IDs created inside AdSense > Ads > By ad unit > Display ads
NEXT_PUBLIC_ADSENSE_SLOT_POPULAR=
NEXT_PUBLIC_ADSENSE_SLOT_FOOTER=

# --- Buy Me a Coffee (donation button in footer) ---
# Create your page at https://buymeacoffee.com and paste your profile URL
NEXT_PUBLIC_DONATE_URL=
